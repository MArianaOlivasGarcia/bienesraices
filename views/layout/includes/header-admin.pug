

header.bg-indigo-600.p-4
    div.container.mx-auto.flex.justify-between.items-center
        h1.text-2xl.text-white.font-extrabold.text-center Bienes 
            span.font-normal Raices
        
        nav(class="my-2 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
            a(href="/my-properties") Mis propiedades
            a(href="") Mi Perfil
            form(method="POST" action="/auth/logout")
                input(type="hidden" name="_csrf" value= csrfToken)
                button(type="submit" class="bg-indigo-800 py-1 px-5") Cerrar sesión